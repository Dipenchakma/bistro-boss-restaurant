import SectionTitle from "../../../Components/SectionTitle";

const Payment = () => {
    return (
        <div><SectionTitle subHeading={''}></SectionTitle>
        </div>
    );
};

export default Payment;